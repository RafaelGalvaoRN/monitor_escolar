/* SUBSTITUA o conteúdo do arquivo headers.css por este */

#novo_background {
  background: linear-gradient(120deg, #155799, #159957);
  width: 100%;
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: space-between; /* Mudança importante aqui */
  padding: 0 40px;
  box-sizing: border-box;
  position: relative;
}

.container-dropdown{
    margin-right: 75px;
}

.container-logo-mp img {
  height: 70px;
  object-fit: contain;
  margin-right: 30px;
}

/* BOTÃO HAMBÚRGUER - POSICIONAMENTO CORRETO */
.mobile-menu-btn {
  display: none; /* Escondido por padrão */
  flex-direction: column;
  justify-content: center;
  align-items: center;
  width: 40px;
  height: 40px;
  background: transparent;
  border: none;
  cursor: pointer;
  padding: 8px;
  gap: 4px;
  z-index: 1001;
  transition: all 0.3s ease;
}

.mobile-menu-btn span {
  display: block;
  width: 25px;
  height: 3px;
  background: #ffffff;
  border-radius: 2px;
  transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  transform-origin: center;
}

/* Animação X quando ativo */
.mobile-menu-btn.active span:nth-child(1) {
  transform: rotate(45deg) translateY(7px);
}

.mobile-menu-btn.active span:nth-child(2) {
  opacity: 0;
  transform: scaleX(0);
}

.mobile-menu-btn.active span:nth-child(3) {
  transform: rotate(-45deg) translateY(-7px);
}

/* NAVBAR */
.navbar {
  flex: 1;
  background: transparent;
  box-shadow: none;
  border: 0;
  position: relative;
  z-index: 10;
}

.navbar-container {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 16px;
  min-height: 70px;
}

.navbar-nav {
  display: flex;
  align-items: center;
  gap: 6px;
  list-style: none;
  margin: 0;
  padding: 0;
}

.nav-item {
  position: relative;
}

.nav-link {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 14px 16px;
  color: #ffffff;
  text-decoration: none;
  font-weight: 600;
  line-height: 1;
  border-radius: 6px;
  transition: background .2s ease, color .2s ease, padding-left .2s ease;
  white-space: nowrap;
}

.nav-link:hover,
.nav-link:focus {
  color: #ffdd57;
  background: rgba(255,255,255,.12);
}

.nav-icon {
  font-size: 16px;
}

/* RESPONSIVE - MOBILE */
@media (max-width: 992px) {

  /* MOSTRA o botão hambúrguer */
  .mobile-menu-btn {
    display: flex !important;
  }

  /* Ajusta o header */
  #novo_background {
    padding: 0 20px;
  }

  /* Posiciona a navbar */
  .navbar {
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    background: transparent;
  }

  /* Menu principal - escondido por padrão */
  .navbar-nav {
    display: none;
    flex-direction: column;
    background: linear-gradient(180deg, #155799 0%, #159957 100%);
    border-radius: 0 0 12px 12px;
    box-shadow: 0 8px 24px rgba(0,0,0,0.2);
    padding: 16px 0;
    margin: 0;
    width: 100%;
    opacity: 0;
    transform: translateY(-20px);
    transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  /* Menu ativo/visível */
  .navbar-nav.active {
    display: flex !important;
    opacity: 1;
    transform: translateY(0);
  }

  /* Items do menu */
  .nav-item {
    width: 100%;
  }

  /* Links do menu */
  .nav-link {
    display: block;
    width: 100%;
    padding: 16px 24px;
    color: #ffffff;
    text-decoration: none;
    border-bottom: 1px solid rgba(255,255,255,0.1);
    transition: all 0.3s ease;
    border-radius: 0;
  }

  .nav-item:last-child .nav-link {
    border-bottom: none;
  }

  .nav-link:hover {
    background: rgba(255,255,255,0.15);
    color: #ffffff;
    padding-left: 32px;
  }
}

/* Resto do CSS original mantido */
.container {
    display: flex;
    width: 100%;
    padding: 4% 2%;
    box-sizing: border-box;
    height: 80vh;
}

.box {
    flex: 1;
    overflow: hidden;
    transition: 0.5s;
    margin: 0 2%;
    box-shadow: 0 20px 30px rgba(0, 0, 0, 0.1);
    line-height: 0;
}

.box > img {
    width: 200%;
    height: calc(100% - 10vh);
    -o-object-fit: cover;
       object-fit: cover;
    transition: 0.5s;
}

.box > span {
    font-size: 1.5vh;
    display: block;
    text-align: center;
    height: 10vh;
    line-height: 2.6;
}

.box:hover {
    flex: 1 1 50%;
}

.box:hover > img {
    width: 50%;
    height: 50%;
}

.iframe-youtube{
    position: relative;
    width: 100%;
    padding-bottom: 20%;
    height: 0;
}

.iframe-container iframe{
    position: absolute;
    top:0;
    left: 0;
    width: 100%;
    height: 100%;
}

@media screen and (max-width: 768px) {
    .container {
      flex-direction: column;
      height: auto;
    }

    .box {
      margin: 1% 2%;
      flex: 1 1 auto;
    }

    .box > img {
      width: 100%;
      height: auto;
    }

    .box > span {
      font-size: 3vh;
    }

    .box:hover {
      flex: none;
    }

    .box:hover > img {
      width: 100%;
      height: auto;
    }
}